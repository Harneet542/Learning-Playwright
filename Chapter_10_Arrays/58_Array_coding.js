let browser = ["chrome","firefox","safari", "edge","opera"];
console.log(browser.length);

browser.pop();  // removes last element
console.log(browser);

// for loop
let browser = ["chrome","firefox","safari", "edge","opera"];
for(let i=0; i<browser.length; i++){
    
    if(browser[i] === "opera"){
        console.log(" Opera! does not support many features");
    }
}


// searching in array 

