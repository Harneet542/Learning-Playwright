let fruit = [];
let browser = ["chrome", "firefox", "webkit"];
console.log(broswer[0]);

//Negative indexing
console.log(browser.at(-1));

//postive can be used like this as well

console.log(browser.at(0));

// Undefined will be if 
console.log(browser[-1]);

// to check lenght 
console.log(browser.length)

// browser.at - at is an function 
// broswe[] - index